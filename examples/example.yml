my-script:
  description: "Does some stuff"
  parameters:
    - id: "profile_id"
      name: "Profile ID"
      type: string
      description: "profile id of user"
  script:
    - "./examples/my_script"
