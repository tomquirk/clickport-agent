my_script:
  name: "Get customer data"
  description: "Retrieve basic data about a customer."
  parameters:
    - id: "profile_id"
      name: "Profile ID"
      flag: "--profile_id"
      description: "The customer's profile ID from Hubspot."
      placeholder: "e.g. 12345"
  script:
    - "go run ./examples/my_script.go"
